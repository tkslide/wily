#!wtcl
wily init
wily attach $argv
while {1} {
	set	event [wily event]
	if [string match [lindex $event 0] WMexec] {
		if [string match [lindex $event 2] Del] {
			wily bounce
			exit
		}
	}
	puts $event
}
