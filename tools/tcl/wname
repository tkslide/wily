#!wtcl
wily init

puts $argc
if [expr $argc < 1 || $argc > 2] {
	puts "usage: $argv0 <id> [<new name>"
	exit 1
}

if [expr $argc == 2 ] {
        wily setname [lindex $argv 0] [lindex $argv 1]
} else {
        puts [ wily name [lindex $argv 0] ]
}
